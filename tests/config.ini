[default]
;todo this is not quite elegant. would need to repeat these two fields for each plugin
plugin_url = http://somedomain.com
plugin_entrypoint = tests.mock_plugin.entrypoint
request_handler_class = tests.app.MainHandler
http_req_wildcard_fields = query, otherfield
paths = plugin_entrypoint, request_handler_class
tuples = http_req_wildcard_fields

[test_dir_a]
request_handler_class = tests.other_app.AnotherHandler
plugin_url = http://someotherdomain.com
paths = request_handler_class

[test_dir_b]
plugin_url = http://anotherdomain.com

