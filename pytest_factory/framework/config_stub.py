# TODO this file is temporary because these configs will need to be pulled from the user's project via ConfigParser
# TODO need to document these somewhere
ASSERT_NO_MISSING_CALLS = False
ASSERT_NO_EXTRA_CALLS = False
HTTP_REQ_WILDCARD_FIELDS = (
    "query"
)
